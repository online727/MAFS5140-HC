Here is the report for strategy version 2.0.

I need to clarify the details of the strategy, which is a combination of multiple feature sets. The feature sets are selected based on their performance in historical data, and the weights are assigned according to their performance metrics.

I think this report should include the following sections:
* Introduction: Briefly describe the motivation and the overall approach of the strategy.
  * Why I choose to use multi-features strategy?
  * What I did to select the feature sets and assign the weights?
    * Compute features and evaluate them separately on the historical data (different categories, parameters, directions), which is implemented in the [`mom_features.py`](../tools/mom_features.py) file.
    * Analyze the performance of each feature set and select the top-performing ones based on different metrics (e.g., Sharpe ratio, drawdown, etc.), which is implemented in the [`feat_ana.py`](../tools/feat_ana.py) file.
    * Implement the strategy by choosing the selected feature sets and assigning weights to them, which is implemented in the [`strategy_ver2.py`](../tools/strategy_ver2.py) and [`strategy_ver2_submit.py`](../tools/strategy_ver2_submit.py) files.
    * Choose the final used feature sets and weights.
      * Final features for each feature set are decided based on the backtest perfomrance of final validation set, saved in [`feature_sets.json`](../feature_sets.json) file.
      * Final weights for each feature set are decided based on their performance on two parts:
        * Train data part: final train set from 2025-01-01 to train_end_date
        * Validation data part: final validation set
        * Final weights are determined by the weighted average of the performance on these two parts (0.7 for train data part and 0.3 for validation data part), saved in [`feature_sets_weights.csv`](../feature_sets_weights.csv) file.
    * Final strategy is [`strategy.py`](../strategy.py). Ituses the top 4 feature sets sorted by `sharpe ratio` metric. The final stock weights are the weighted average (weighted by their `sharpe ratio`) of the weights generated by these 4 feature sets.
* Methodology
  * Feature computation: Describe how the features are computed and what they represent.
  * Feature evaluation: Describe how the features are evaluated.
  * Feature selection and weighting: Describe how the feature sets are selected and how the weights are assigned.
  * Strategy implementation: Describe how the strategy is implemented using the selected feature sets and weights.
* Results: Present the results of the backtest, including performance metrics and comparisons with benchmarks.
* Conclusion: Summarize the findings and discuss potential improvements or future work.
